{
  "name": "middle-ground-functions",
  "description": "Cloud Functions for Middle Ground push notifications",
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "scripts": {
    "_comment_test": "Explicit glob rather than `node --test test/`: Node 26 resolves a bare directory as a module path and dies with MODULE_NOT_FOUND. The glob works on 20 and 26 alike.",
    "test": "node --test \"test/*.test.js\"",
    "test:rules": "cd .. && firebase emulators:exec --only firestore \"cd CloudFunctions && npm test\""
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.0.0"
  },
  "private": true
}
